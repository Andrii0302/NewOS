#include "newos.h"
#include "stdlib.h"
#include "stdio.h"
#include "string.h"
int main(int argc, char** argv)
{   
    
    // print("Hello how are you!\n");
    // print(itoa(8763));
    // putchar('V');
    // void* ptr = malloc(512);
    // free(ptr);
    // char buf[1024];
    // newos_terminal_readline(buf, sizeof(buf), true);

    // print(buf);
    // char words[] = "hello how are you";

    // const char* token = strtok(words, " ");
    // while(token)
    // {
    //     printf("%s\n", token);
    //     token = strtok(NULL, " ");
    // }
    // char* ptr = malloc(20);
    // strcpy(ptr, "hello world");

    // print(ptr);
    // free(ptr);

    // ptr[0] = 'B';
    // print("abc\n");
    // char str[] = "hello world";
    // struct command_argument* root_command = newos_parse_command(str, sizeof(str));
    // printf("%s\n", root_command->argument);
    // printf("%s\n", root_command->next->argument);
    // struct process_arguments arguments;
    // newos_process_get_arguments(&arguments);

    // printf("%i %s\n", arguments.argc, arguments.argv[0]);
    // print(argv[0]);
    // print("did this work?\n");
    // for (int i = 0; i < argc; i++)
    // {
    //     printf("%s\n", argv[i]);
    // }
    while(1)
    {
        
        print(argv[0]);
        for (int i = 0; i < 1000000; i++)
        {

        }
    }
    // char* ptr = (char*) 0x00;
    // *ptr = 0x50;


    // while(1) 
    // {
        
    // }
    return 0;
}